  <form action="" method="post" enctype="multipart/form-data">
  <div class="mb-3">
      <label for="categoryId">Catégorie</label>
      <select name="categoryId" id="categoryId" class="form-select">
          @if (categories)
              @each(category in categories)
                  <option value="{{ category.id }}" {{ (post && post.categoryId == category.id) ? 'selected' : '' }}>
                      {{ category.name }}
                  </option>
              @endeach
          @endif
      </select>
  </div>




    <div class="mb-3">

        @!component('components/input', {
            label: 'Titre',
            name: 'title',
           value: post && post.title ? post.title : ''
        })
        </div>
    <div class="mb-3">
        @!component('components/input', {
                    label: 'Contenu',
                    name: 'content',
                    value: post && post.content ? post.content : '',
                    type : 'textarea'
                      // Correction ici
                })
    </div>

      <div class="mb-3">
            @!component('components/input', {
                        label: 'demo',
                        name: 'demo',
                        value: 'demo',
                          // Correction ici
                    })
        </div>

        <div class="mb-3">

   <input {{post && post.online  ? "checked" : ''}}  type="checkbox" name="online" class="form-check-input" id="online">
   <label for="online" class="form-check-label">En ligne </label>
        </div>

        <button class="btn btn-primary">Sauvegarder</button>

    </form>
